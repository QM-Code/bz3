# src/engine/karma/audio/backends/miniaudio/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `audio/backends/miniaudio`.

It should only contain forwarding headers that include the real implementation
from `src/engine/audio/backends/miniaudio`.
