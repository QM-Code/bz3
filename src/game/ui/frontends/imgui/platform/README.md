# ImGui Platform Renderers

Renderer backends for ImGui. These provide the render-to-texture bridge used by the graphics backends.

Files:
- `renderer_bgfx.*`
- `renderer_diligent.*`
- `renderer_forge.*`
