# src/engine/karma/physics/backends/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `physics/backends`.

It should only contain forwarding headers that include the real implementation
from `src/engine/physics/backends`.
