# src/engine/karma/world/backends/fs/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `world/backends/fs`.

It should only contain forwarding headers that include the real implementation
from `src/engine/world/backends/fs`.
