# src/engine/karma/input/mapping/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `input/mapping`.

It should only contain forwarding headers that include the real implementation
from `src/engine/input/mapping`.
