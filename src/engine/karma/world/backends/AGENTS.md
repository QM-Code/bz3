# src/engine/karma/world/backends/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `world/backends`.

It should only contain forwarding headers that include the real implementation
from `src/engine/world/backends`.
