# src/engine/karma/ui/bridges/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `ui/bridges`.

It should only contain forwarding headers that include the real implementation
from `src/engine/ui/bridges`.
