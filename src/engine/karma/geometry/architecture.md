# src/engine/karma/geometry/architecture.md

This is a forwarder-only directory. It mirrors the public engine API layout so
that game code can include `karma/geometry/...` while Karma is still in-tree.
