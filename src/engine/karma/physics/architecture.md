# src/engine/karma/physics/architecture.md

This is a forwarder-only directory. It mirrors the public engine API layout so
that game code can include `karma/physics/...` while Karma is still in-tree.
