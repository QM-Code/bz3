# src/engine/karma/physics/backends/physx/AGENTS.md

Read `src/engine/karma/AGENTS.md` first.
This directory is part of the Karma **forwarder tree** for `physics/backends/physx`.

It should only contain forwarding headers that include the real implementation
from `src/engine/physics/backends/physx`.
