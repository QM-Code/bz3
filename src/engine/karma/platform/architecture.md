# src/engine/karma/platform/architecture.md

This is a forwarder-only directory. It mirrors the public engine API layout so
that game code can include `karma/platform/...` while Karma is still in-tree.
