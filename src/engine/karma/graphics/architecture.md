# src/engine/karma/graphics/architecture.md

This is a forwarder-only directory. It mirrors the public engine API layout so
that game code can include `karma/graphics/...` while Karma is still in-tree.
