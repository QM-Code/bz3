# src/engine/app/README.md

This directory defines Karma’s runtime shell (`EngineApp`).
It owns subsystem startup/shutdown and exposes a context that games can use.

If you are integrating a new game loop or system, this is the entry point.

See `src/engine/app/AGENTS.md` for detailed guidance.
